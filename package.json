{
  "name": "behavior-chain-site",
  "version": "1.0.0",
  "description": "Has a behavior chain template that then renders a completed behavior chain for user. ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jrosecow",
  "license": "ISC"
}
